spring:
    application:
     name: gateway-server
    profiles:
      active: default
    cloud:
        config: 
            uri: http://configserver:8888
    config:
      import: configserver:http://configserver:8888